JSX lets us write HTML-like code inside JavaScript. It makes React UI easier to write.
Props are data passed from parent to child. State is data managed inside a component.
usestate stores and updates data in a component. I used it to manage the selected technology stack.
useEffect runs code after rendering. It can be used to fetch JSON data from the server.
A unique key helps React identify each item and update the list efficiently.
Conditional rendering means showing different UI based on a condition.
A parent passes data using props. A child can send data back by calling a function passed through props.
